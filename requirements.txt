# Phase 1 + Phase 2: 표준 라이브러리만 사용 (urllib, json, sqlite3, math)
# 추가 의존성 없음
yfinance>=0.2
# Brave Search API: urllib.request 직접 사용

# 테스트
pytest>=7.0
ruff>=0.4.0

# Phase 3 (예정)
# matplotlib      # 차트 이미지 생성
# schedule        # 크론 스케줄러
